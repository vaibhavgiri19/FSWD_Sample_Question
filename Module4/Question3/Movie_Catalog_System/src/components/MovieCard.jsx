import React from 'react';

function MovieCard({ title, genre, rating, isAvailable, isTopRated }) {
  return (
    <div className="movie-card">
      {/* TODO: Display title */}
      {/* TODO: Display genre */}
      {/* TODO: Display rating */}
      {/* TODO: Conditionally show 'TOP RATED' badge if isTopRated is true */}
      {/* TODO: Conditionally render availability status */}
    </div>
  );
}

export default MovieCard;
