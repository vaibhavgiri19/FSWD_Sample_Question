import React from 'react';

const ProductCard = ({ name, brand, price, originalPrice, category, inStock, discount }) => {
  return (
    <div className="product-card">
      {/* Show DISCOUNT badge when discount is true */}
      {discount && <span className="discount-badge">DISCOUNT</span>}

      <h2>{name}</h2>
      <p>{brand}</p>

      <p>
        <span className="price">₹{price}</span>
        {originalPrice > price && (
          <span className="original-price">₹{originalPrice}</span>
        )}
      </p>

      <p className={`stock-status ${inStock ? 'available' : 'unavailable'}`}>
        {inStock ? 'In Stock' : 'Out of Stock'}
      </p>
    </div>
  );
};

export default ProductCard;




///App.jsx

import React from "react";
import ProductCard from "./ProductCard";

const App = () => {
  const products = [
    {
      id: 1,
      name: "Wireless Earbuds",
      brand: "SoundMagic",
      price: 2999,
      originalPrice: 3999,
      inStock: true,
      isFeatured: true,
    },
    {
      id: 2,
      name: "Smart Watch",
      brand: "TechTime",
      price: 4999,
      originalPrice: 5999,
      inStock: false,
      isFeatured: false,
    },
    {
      id: 3,
      name: "Gaming Mouse",
      brand: "HyperClick",
      price: 1999,
      originalPrice: null,
      inStock: true,
      isFeatured: true,
    },
  ];

  return (
    <div>
      <h1>Product Catalog</h1>
      <div className="product-list">
        {products.map((item) => (
          <ProductCard
            key={item.id}
            name={item.name}
            brand={item.brand}
            price={item.price}
            originalPrice={item.originalPrice}
            inStock={item.inStock}
            isFeatured={item.isFeatured}
          />
        ))}
      </div>
    </div>
  );
};

export default App;
